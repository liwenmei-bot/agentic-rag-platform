# 截图目录

把展示项目效果的截图放在这里，命名建议：

- `chat.png` —— 知识库问答界面（带引用来源）
- `graph.png` —— 知识图谱可视化
- `agent.png` —— Agent 模式的工具调用过程
- `docker.png` —— Docker Compose 启动成功的终端截图

放好之后，去根目录 `README.md` 的"功能演示"部分，取消 HTML 注释（删掉 `<!--` 和 `-->`），图片就会显示在仓库首页。

建议截图尺寸不要太大（单张控制在 500KB 以内），GitHub 加载更快，也不会让仓库体积膨胀太多。
